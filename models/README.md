# Models Folder

This folder contains small sample versions of trained models.

Full models are not included due to size constraints.

To reproduce:
1. Run `src/preprocessing.py`
2. Run `src/train_model.py`
3. Models will be saved here automatically.
