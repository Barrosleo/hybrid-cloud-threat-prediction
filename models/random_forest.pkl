This is a placeholder file.  
The actual Random Forest model (.pkl) is not included in the repository due to size and security considerations.

To generate this file, run:

python src/train_model.py

The trained model will be saved automatically to:
models/random_forest.pkl
